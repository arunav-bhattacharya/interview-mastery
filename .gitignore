# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# tsc emit sidecars — sources are TypeScript only; .js next to .ts/.tsx is a
# stale compile artifact (tsconfig sets noEmit:true but older sessions left some).
/docusaurus.config.js
/sidebars.js
/src/**/*.js

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.idea/
.vscode/

# Don't commit yarn.lock (project uses npm)
yarn.lock
